<div>
  <%= button_to "Log out", session_path, class: "text-black dark:text-white", method: :delete if authenticated? %>
  <%= link_to "Login", new_session_path, class: "text-black dark:text-white" unless authenticated? %>
  <% if authenticated? %>
    <h1 class="font-bold text-4xl text-black dark:text-white">Welcome to Dietly, <%= Current.user.first_name %>!</h1>
  <% else %>
    <h1 class="font-bold text-4xl text-black dark:text-white">Welcome to Dietly!</h1>
  <% end %>
  <p class="text-black dark:text-white"dark>Find me in app/views/home/show.html.erb</p>

</div>
